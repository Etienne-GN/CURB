# Rust build output
/target
**/*.rs.bk
Cargo.lock.orig

# eBPF
/bpf/target
# The compiled .bpf.o is checked in so the repo builds without clang being present
# at build time. Regenerate with: cd curbd && cargo build (requires clang).

# Tauri / GUI
/gui/node_modules
/gui/dist
/gui/src-tauri/target
/gui/src-tauri/gen/schemas

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Local dev socket / runtime state
/run/
*.sock

# Temp icon processing artifacts
icon_padded.png
